pipeline {
    agent any

    environment {
        NODE_ENV = 'test'
    }

    tools {
        nodejs 'node18'   // لازم يكون متضاف في Jenkins Tools
    }

    stages {

        stage('Install Dependencies') {
            steps {
                dir('frontend') {
                    sh 'npm install'
                }
